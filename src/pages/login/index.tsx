import Button from "@/components/Button";
import React from "react";

const LoginPage: React.FC = () => {
  return (
    <div
      id="LoginPage"
      className="w-screen h-screen flex flex-col justify-center items-center gap-5 p-5"
    >
      <Button />
    </div>
  );
};

export default LoginPage;
